openapi: 3.0.4
info:
  title: Card Validator
  description: >-
    Card Validator checks whether a card number is valid, identifies the card
    brand, and flags risk. It runs the Luhn checksum, detects the scheme (Visa,
    Mastercard, Amex and more), returns the PCI-safe BIN and last 4 digits,
    flags known processor test cards, and returns a composite risk score.
  termsOfService: https://apiverve.com/terms/
  contact:
    name: APIVerve Support
    email: hello@apiverve.com
    url: https://apiverve.com/contact
  version: 1.0.0
externalDocs:
  description: Learn more about APIVerve
  url: https://docs.apiverve.com?utm_source=openapi
servers:
  - url: https://api.apiverve.com/
paths:
  /v1/cardvalidator:
    get:
      summary: Validate Card Number
      description: Validate a card number to ensure it is valid
      operationId: cardvalidator
      parameters:
        - name: number
          in: query
          schema:
            type: string
          required: true
          description: The card number to validate
          example: '4900264223817524'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: status
                  error:
                    type: string
                    description: error
                  data:
                    type: object
                    description: data
              example:
                status: ok
                error: null
                data:
                  card:
                    niceType: Visa
                    type: visa
                    patterns:
                      - 4
                    gaps:
                      - 4
                      - 8
                      - 12
                    lengths:
                      - 16
                      - 18
                      - 19
                    code:
                      name: CVV
                      size: 3
                    matchStrength: 1
                  brand: Visa
                  cardNumber: '4900264223817524'
                  bin: '490026'
                  last4: '7524'
                  isValid: true
                  isPotentiallyValid: true
                  isTestCard: false
                  riskScore: 0
                  riskLevel: low
        '401':
          description: Your request was not authorized, or your API Key was invalid
        '404':
          description: Requested resource was not found
        '500':
          description: A server error has occurred, try again later
components:
  securitySchemes:
    api_key:
      type: apiKey
      name: x-api-key
      in: header
security:
  - api_key: []
